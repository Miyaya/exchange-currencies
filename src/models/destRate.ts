class DestRate {
    currencyName: string;
    rate: number;

    constructor(currencyName: string, rate: number) {
        this.currencyName = currencyName, this.rate = rate
    }
}

export default DestRate;